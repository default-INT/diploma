export default class DataItem {
    constructor(id, name, value, iconUri, color) {
        this.id = id
        this.name = name
        this.value = value
        this.iconUri = iconUri
        this.color = color
    }
}